language: ruby
rvm:
    - 2.2.3
addons:
  postgresql: "9.4"

  services:
    - postgresql
env:
    - DB=postgresql
script:
    - bundle exec rake db:test:prepare
    - bundle exec rspec spec/
    

    
